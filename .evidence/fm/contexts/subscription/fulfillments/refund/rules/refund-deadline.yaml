type: rule
id: rule.refund-deadline
kind: derivation
label: 计算断更退款请求截止
description:
  "以断更退款请求（request.refund）的开始时间为基准增加订阅合同（contract.subscription）\
  约定的退款秒数，得到该请求的截止时间。"
contextRef: fulfillment.refund
bindings:
  agreement:
    ref: contract.subscription
  request:
    ref: request.refund
expression: |-
  request.started_at + duration(string(agreement.refund_seconds) +
  "s")
resultType: timestamp
target:
  entityRef: request.refund
  attribute: expired_at
