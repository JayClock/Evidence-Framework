type: rule
id: rule.restore-deadline
kind: derivation
label: 计算免费恢复请求截止
description:
  "以免费恢复请求（request.restore）的开始时间为基准增加订阅合同（contract.subscription）\
  约定的恢复秒数，得到该请求的截止时间。"
contextRef: fulfillment.restore
bindings:
  agreement:
    ref: contract.subscription
  request:
    ref: request.restore
expression: |-
  request.started_at + duration(string(agreement.restore_seconds) +
  "s")
resultType: timestamp
target:
  entityRef: request.restore
  attribute: expired_at
