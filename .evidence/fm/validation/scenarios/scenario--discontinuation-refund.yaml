type: fm_scenario
id: scenario.discontinuation-refund
label: 断更下架后全额退款
asOf: '2026-10-18T10:01:00Z'
givenInstanceRefs:
  - instance.subscription
  - instance.mobile-agreement
  - instance.payment
  - instance.mobile-request
  - instance.mobile-result
steps:
  - sequence: 1
    actingRoleRef: role.publisher
    issueInstanceRef: instance.discontinuation
    availableInstanceRefs:
      - instance.subscription
      - instance.mobile-agreement
      - instance.payment
      - instance.mobile-request
      - instance.mobile-result
  - sequence: 2
    actingRoleRef: role.reader
    issueInstanceRef: instance.refund
    availableInstanceRefs:
      - instance.subscription
      - instance.mobile-agreement
      - instance.payment
      - instance.mobile-request
      - instance.mobile-result
      - instance.discontinuation
  - sequence: 3
    actingRoleRef: role.publisher
    issueInstanceRef: instance.refunded
    availableInstanceRefs:
      - instance.subscription
      - instance.mobile-agreement
      - instance.payment
      - instance.mobile-request
      - instance.mobile-result
      - instance.discontinuation
      - instance.refund
evaluations:
  - ruleRef: rule.payment-start
    bindings:
      agreement:
        instanceRef: instance.subscription
    targetInstanceRef: instance.payment
    expectedResult: '2026-10-01T09:00:00Z'
  - ruleRef: rule.payment-amount
    bindings:
      agreement:
        instanceRef: instance.subscription
    targetInstanceRef: instance.payment
    expectedResult: 9900
  - ruleRef: rule.payment-deadline
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.payment
    targetInstanceRef: instance.payment
    expectedResult: '2026-10-01T09:15:00Z'
  - ruleRef: rule.refund-deadline
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.refund
    targetInstanceRef: instance.refund
    expectedResult: '2026-10-23T10:01:00Z'
  - ruleRef: rule.refund-amount
    bindings:
      agreement:
        instanceRef: instance.subscription
    targetInstanceRef: instance.refund
    expectedResult: 9900
  - ruleRef: rule.refund-completed
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.refund
      payments:
        instanceRefs:
          - instance.mobile-result
      notices:
        instanceRefs:
          - instance.discontinuation
      confirmations:
        instanceRefs:
          - instance.refunded
    expectedResult: true
  - ruleRef: rule.refund-expired
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.refund
      payments:
        instanceRefs:
          - instance.mobile-result
      notices:
        instanceRefs:
          - instance.discontinuation
      confirmations:
        instanceRefs:
          - instance.refunded
    expectedResult: false
  - ruleRef: rule.payment-completed
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.payment
      proofs:
        instanceRefs:
          - instance.mobile-result
    expectedResult: true
  - ruleRef: rule.payment-expired
    bindings:
      agreement:
        instanceRef: instance.subscription
      request:
        instanceRef: instance.payment
      proofs:
        instanceRefs:
          - instance.mobile-result
    expectedResult: false
expectations:
  fulfillmentStatuses:
    - fulfillmentRef: fulfillment.refund
      requestInstanceRef: instance.refund
      status: completed
    - fulfillmentRef: fulfillment.payment
      requestInstanceRef: instance.payment
      status: completed
stakeholderReview:
  status: pending
notes: 以固定业务时刻和独立凭证回放；仅使用当步已有且可见的证据。
