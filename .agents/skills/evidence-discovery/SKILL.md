---
name: evidence-discovery
description: 业务访谈与领域语言沉淀：读取材料和已有记录，逐问真正影响判断的缺口，保存回答、来源、更正及停止／暂缓状态，并按 FM 方法当轮更新已明确术语的统一词汇表。用户希望讨论、澄清、继续访谈或整理交接时使用；不自动修改模型 JSON、规则或验证场景。
compatibility: 对话与文本读取；保存记录和术语需要项目写入权限。通用访谈可独立使用，领域语言沉淀及 FM 专业判断需可读取 evidence-fm 参考。
---

# Evidence 业务访谈与领域语言沉淀

负责把问题问清楚，并让已经明确的领域语言立即进入后续对话。访谈机制由本包维护，术语判断与写入方法由 `evidence-fm` 维护；两者在每轮协作，不等待访谈全部结束。

## 1. 读取材料、词汇表与适用准则

读取用户材料、现有问题、访谈记录及统一词汇表；恢复时承接原来的焦点和状态。访谈机制只在 [访谈方法](references/interview.md) 维护。

通过宿主的资源发现能力定位已安装的 `evidence-fm`，或读取用户提供的包路径；按本轮任务读取参考：

| 判断涉及什么                           | FM 包内读取位置                     |
| -------------------------------------- | ----------------------------------- |
| 领域语言、术语区别、增量写入与更正     | `references/domain-language.md`     |
| 合同履约、领域身份、渠道协商与责任缺口 | `references/business-analysis.md`   |
| 关键数据的业务来源与时间含义           | `references/provenance.md`          |
| 业务案例与模型验证边界                 | `references/scenario-validation.md` |

这些是资源入口，不是假设存在的兄弟目录或专门工具。缺少 FM 准则时，继续一般事实澄清并保存原话与待沉淀项，明确尚未更新词汇表；不复制专业方法、不凭记忆补写格式、不要求用户用业务回答弥补知识包缺失，也不宣称完成专业模型审查。

## 2. 每轮澄清并沉淀

1. 消化全部新增材料、回答、更正及控制意图，复用已知事实和有效依据。
2. 先声明“业务主线 → 上下文 → 当前履约／协商／领域对象”，给出简短工作理解。对照词汇表指出模糊词、同名异义和概念区别；推断与未知明确标记。
3. 先保存原话与来源，再按 FM 的领域语言方法将含义明确的术语当轮写入统一词汇表。默认使用项目根 `.evidence/glossary.json`，按独立词汇表 Schema 维护严格 JSON，不创建 Markdown 词典；未决解释留在发现记录。词汇表保存失败立即停止，分别报告两处写入结果。
4. 围绕当前切片核对请求及确认凭证的责任角色、期限、参与者／代表／凭证提供者、标的物和异常责任。可以使用明确标成假设的具体案例检验概念边界，不将其当成业务事实。
5. 说明哪个判断仍不能作出、缺什么依据、回答会改变什么；只问一个必要业务问题并等待真人。保存本轮记录与术语后才继续下一问；信息足够时整理并停止，不为模板凑题。

缺口可以来自用户叙述或 FM 输入复核。业务未知、技术表达与资源安装分开；访谈不替用户作决定，不因存在术语就创建 Entity 或规则。

## 3. 保存依据与授权边界

按 [访谈记录模板](assets/discovery-template.json) 用 JSON 更新项目已有记录；没有时告知默认使用项目根的 `.evidence/discovery.json`，需要拆出已提问题时使用 `.evidence/questions.json`，主记录用 `questionsFile` 指向它，同一问题不在两处重复维护。只保留有内容的部分，键语义以模板为准；保存原话与来源定位、临时理解、实际问题和控制状态。已沉淀术语只引用词汇表条目及来源，不复制定义正文；讨论案例仍是材料，不自动生成验证套件。

调用本访谈流程默认包含发现记录和已明确术语的增量写入；“只聊不落盘”“仅保存资料”“只记录冲突”等更窄要求优先。只修改本次授权的文件，不改变业务确认记录，不自动迁移、删除或重编号已有记录。术语写入不授权修改模型 JSON、关系、规则、验证场景、API 或实现。没有写入权限时给可复制内容并明确未保存。

## 4. 结束与交接

按访谈方法处理停止、暂缓和继续；停止不解除已授权的本轮保存职责，也不扩大写入范围。交接只给当前焦点、来源与问题定位、词汇表增改条目及保存结果、有效控制状态和下一步选择。新增原话先写回记录，已有正文只引用。

用户明确要求生成／更新模型 JSON 时，可切换到 `evidence-fm` 消费当前词汇表和有效来源，编辑 `.evidence/fm/` 并校验；交接只引用当前模型及 `.evidence/checks/fm/` 中实际保存的结果。收敛软件范围时可选择 `evidence-requirements`。结束访谈不是执行下游任务的授权，FM 返回缺口也不自动恢复已停止的访谈。

参考只随当前问题按需读取；evals/ 仅用于维护或明确评测，不作为本项目的业务来源。
