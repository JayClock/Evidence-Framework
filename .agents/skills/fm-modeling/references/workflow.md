# 直接编辑与校验工作流程

## 文件布局与恢复

默认路径均相对项目根，按需创建；沿用已有文件与用户显式指定路径，不自动迁移：

- `.evidence/discovery.md`：来源、回答、工作理解、问题、暂缓／停止状态与交接；需拆分时用 `.evidence/questions.md`。
- `.evidence/fm/`：唯一当前模型，源 YAML、术语、说明、validation 与 generated。
- `.evidence/checks/fm/`：获授权保存的真实检查记录，绑定模型内容摘要。
- `.evidence/api/api.yaml`：另行使用 `fm-api-design` 时维护的 API 设计源。
- `.evidence/api/generated/<批次>/`：API 投影的新输出目录。
- `.evidence/checks/api/`：获授权保存的 API 检查记录。

恢复时读取项目文件，不猜测旧会话。发现记录只保存当前焦点、来源、实际问题及状态、模型和检查指针，不复制模型正文。模型可能处于编辑中或校验失败状态，不能仅凭文件存在判定可用；下游消费前重新校验。

## 讨论与澄清

遵循 `evidence-discovery`。未形成模型也可以先问必要业务问题；一次回答中的全部事实和更正都要消化。写入失败时明确尚未保存并停止，不能继续提问。

停止不会因后续材料自动解除；暂缓问题沿用原 Q-ID 与 gapKey。材料足够时保存交接后停止，不为凑流程提问。讨论、普通回答与停止不是修改模型的授权。

## 生成或修改

用户要求生成或修改即授权编辑本次范围内的当前模型。先读取现有文件和 Git 差异（若有），区分已有改动与本轮变更，不覆盖无关工作。

遵循 `evidence-fm`，按事实依赖评估 ready、support、pending。直接编辑 `.evidence/fm/`，保留未受影响的有效内容和稳定 ID。撤回对象时同步处理引用，真实未知留在评估中；没有可纳入职责时只记录依据与缺口。

运行 `check_fm.py`。失败时只修复获授权的表达错误，不削弱业务预期；无法解决则明确当前模型未通过、哪些文件已经修改及下一步选择。用户中断后不自动回滚，下次从当前文件继续。

最后展示：

1. 模型路径与本次实际增改删，提供差异及新文件内容；Git diff 不包含未跟踪文件，须单独检查。
2. 变更来源、事实依赖和未纳入职责。
3. Schema、CEL、lineage、simulation 与 timeline 的真实检查结果，声明与实际执行场景数。
4. `modelDigest`、`inputChanged`、Evidence 时间线及未决顺序。
5. 仍影响业务判断的缺口与当前审核状态。

差异展示不是额外的写入确认步骤。写入与机器通过都不是具名业务批准，模型保持有依据的审核状态。Git 仅用于查看和用户明确要求的版本操作，不自动暂存、提交或回滚。

## 只读校验与检查记录

只校验时运行 FM 包内 `check_fm.py`，报告真实退出状态；不写任何项目文件。没有实际执行场景时 `simulationPassed` 为 null，不宣称模拟通过。

用户要求留存结果时，将真实输出保存到 `.evidence/checks/fm/<运行标识>.json`，并按 FM 校验方法保留命令、环境与来源版本指针。不覆盖已引用的检查记录，不将报告放进模型目录。来源、模型、场景或校验器变化后重跑；旧结果只证明旧版本。

## 结束与界面降级

提问界面只收集回答、暂缓或停止选择，业务标识与记录仍由 Agent 维护。无界面时用普通对话等待输入；取消或停止不伪造答案、不主动发下一题。

交接只给当前焦点、模型路径、实际检查记录与未决项；没有保存报告就明确未保存。完成后停止，不自动启动 API、需求、架构、开发或业务审核。
