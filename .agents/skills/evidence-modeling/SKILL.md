---
name: evidence-modeling
description: 显式组合经营分析、合约权责、凭证追溯、单据推演、参与者与角色扮演发现及领域语言沉淀，另按授权生成／修改／校验 FM 模型 JSON。用户希望从材料识别上下文、澄清术语或维护业务模型时使用；区分术语写入、模型编辑和只读校验。
disable-model-invocation: true
compatibility: 对话、文件读写与命令执行；建模与校验需要可发现的 evidence-fm，专业访谈建议同时提供 evidence-discovery。
---

# Evidence Modeling 组合入口

这是用户显式调用的编排入口。它把访谈与领域语言沉淀组合在每一轮，按另行授权进入模型 JSON 编辑和校验，不另造一套 FM 语义：访谈机制归属 `evidence-discovery`，术语方法、模型与当前 Schema 的判断归属 `evidence-fm`。

## 开始

读取用户目标、项目约定、发现记录、当前模型、检查记录及已有 Git 差异。新产物默认放在项目根的 `.evidence/`，具体布局和恢复方式见 [工作流程](references/workflow.md)。沿用已有文件与用户显式指定路径，不自动迁移。只修改本次授权的文件，不改变业务确认记录。

通过宿主资源发现定位已安装 Skill，不假设兄弟目录或固定安装路径。识别本次意图：

- **访谈并沉淀领域语言**：沿经营目标与材料、合约权责、凭证追溯、单据推演、参与者与角色扮演、变化点与边界共同理解业务，当轮沉淀未建模术语，已建模概念只建立 FM 引用，不复制名称和含义；只授权发现记录和词汇表，不修改模型 JSON、关系、规则、验证场景、API 或实现。只聊不落盘、仅保存资料或只记录冲突等更窄要求优先。
- **生成／修改模型**：用户请求即为本次范围内的编辑授权；先完成必要业务分析，再直接编辑 `.evidence/fm/`，校验并展示差异。
- **只校验**：运行只读检查，不修改词汇表、模型、场景、发现记录或报告文件。
- **生成可视化审核页**：定位 `evidence-visualization` 并读取其 `SKILL.md`，从重新校验的 FM／已有 API 生成离线只读 HTML；不重复维护展示方法，不修改业务模型或自动创建 API。
- **暂停／结束**：按本次授权保存已取得的依据、已明确术语和控制状态后停止，不继续追问或启动模型 JSON 编辑。

资源缺失是执行限制，不是业务问题。缺少 `evidence-discovery` 时可以消费充分材料，但不自创访谈机制；缺少 `evidence-fm` 时不能声称完成专业建模或校验。只读环境给出可复制交接并明确未保存。

## 先选分析路径

在提出问题或映射 Schema 前，先判断当前复杂度来自哪里：

- **业务逻辑**：围绕收入、成本、绩效、合规或风险，优先沿协议、双方权责和履约证据分析。
- **领域逻辑**：围绕算法、计划、统计、优化、身份和领域结构，直接分析 Domain Context，不补造合同。
- **工具／胶水**：仅连接已有能力且没有独立业务或领域语义时，可以判断 FM 不适用；信息不足不是不适用。
- **混合问题**：先分离运营相关的业务逻辑与问题域自身的领域逻辑，再通过有来源的 Evidence 与 Thing／Role 关系组合。

这一步选择分析路线，不替代 `evidence-fm` 的适用性与 ready／support／pending 判断。

## 组合流程

1. **取得依据并承接分析**：处理全部新增材料、回答和更正，先保存原话与来源。遵循 `evidence-discovery` 的访谈机制和 FM 的 `references/business-analysis.md`，从经营目标与真实材料理解权责；缺约定先找适用合约或专家说明，无材料才从真实凭证逆推。新业务设计需另行授权，建议不当成现行事实。
2. **展示并推演业务理解**：维护可读业务视图，沿主张、依据、完成证明与后续责任追查关键数据、版本和时间。凭证链可解释时用正常及争议单据推演检验能否定责，记录实际结果与缺口。按 `references/domain-language.md` 当轮维护 standalone 定义或 model 引用；没有模型编辑授权的更正仅记录影响，不另写定义。两处保存任一失败即停止并交接。合同前保留独立渠道，领域与工具按对应分支分析，不套合同。
3. **寻找玩家并识别业务变化**：按 FM 业务分析方法沿 Party Role 主动寻找实际主体，核对跨上下文同一性、代表依据与未知项，写入发现记录的 `participants`；已有依据立即复用，不以推演全部完成为关卡。访谈不写正式 `plays_role`。随后检查可替换的确认凭证、不同签约渠道、不同履约弹性，以及被业务消费的领域输入。变化点必须来自业务结构，不从 API、队列、数据库或当前服务拆分反推。
4. **正式映射**：用户要求生成或修改模型 JSON 时遵循 `evidence-fm`，解析当前词汇条目的定义拥有者及来源，评估 ready、support、pending，直接维护一个当前模型目录。目录、命名与结构以当前 FM Schema 和 `evidence-fm` 的格式方法为准；简化分析不能覆盖现行约束。落实独立术语后保留 term ID，将条目替换为 FM 引用，不留第二份名称与定义。
5. **真实校验**：仅在模型 JSON 编辑或只读校验任务中运行 Schema、CEL、lineage、simulation 与 timeline 检查；纯访谈与术语沉淀不运行模型校验。只修正授权范围内的表达错误，不削弱业务预期；失败时如实保留当前文件和错误，不自动回滚。
6. **展示结果**：访谈列出经营依据、当前业务视图、实际单据推演、参与者与角色扮演、变化点与边界、术语引用及各文件保存结果；独立术语任务只列本次术语变更。对话推演不等于机器校验，未执行真人推演不声称已验证业务理解。模型任务另列实际增改删、检查摘要、`modelDigest`、Evidence 时间线、变化点与未决顺序。文件变化后旧检查不能证明当前模型有效。
7. **保存交接并停止**：仅保存获授权的最小交接。Git 用于查看差异和版本恢复，不自动提交、暂存或回滚；不自动进入 API、需求、架构或开发。

## 不可混淆的业务语义

- Request 表示权利方向义务方提出的、具有业务时限的履约主张；Confirmation 表示由实际提供方形成、证明具体履约结果的凭证。
- Request 已形成而合格 Confirmation 尚未形成时，业务上处于待履约／未知结果，不把它压缩成同步技术调用。
- 回调到达、记录创建、人工审批和履约确认不是天然同一事件；必须按来源分别判断。
- 失败、取消、退款、冲正、补偿或赔偿可能触发新的 Evidence／Fulfillment；不修改原凭证来伪装业务修正，也不把未知后果写成无责任。
- 现实当事人可作为发现入口；正式模型仍按当前 Schema 区分 Participant、Party Role、Evidence Role 及责任归属。

界面工具仅用于提问；不可用时转普通对话。差异和检查结果通过普通对话展示，不能把界面回答当作文件已经保存。写入、机器检查与业务批准始终相互独立。
