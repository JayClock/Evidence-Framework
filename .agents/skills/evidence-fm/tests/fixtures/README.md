# FM 测试夹具

本目录保存 FM Skill 的合成测试数据，仅用于功能回归、可移植性测试和评测准备，不是业务默认值，也不是项目 `.evidence/fm/` 的事实来源。

- `domain/`：无合同的纯领域模型，用于校验和编译边界。
- `valid-traceable-subscription/`：包含订阅付款、证据追溯和成功／逾期场景的完整模型，用于运行模拟。
- 其他目录：针对具体规则、上下文和负例的回归夹具。

测试需要修改模型时，应先复制到临时工作目录；不要直接修改本目录中的业务预期。
