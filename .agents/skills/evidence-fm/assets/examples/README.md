# 合成示例，不是业务默认值

- [domain](domain/model.yaml)：无合同、无 Evidence 的客户档案类型与编辑前置规则。规则仅为本示例设定；校验和编译不等于执行了档案编辑或状态机，模拟结果应为 null。
- [payment](payment/model.yaml)：订阅付款、关键属性派生及成功／逾期场景。金额、期限和角色仅是测试条件，不得复制为用户业务事实。

可直接只读校验；需要修改或生成输出时，先复制到用户允许的临时工作目录。两个模型均保持 draft / pending，不代表业务批准。
