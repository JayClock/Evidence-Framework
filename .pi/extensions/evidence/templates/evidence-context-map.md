---
description: 从统一模型设计上下文边界与集成映射，不预设独立 DDD 工件
---

# 上下文映射

## 边界设计

读取 Modeling 的统一语言、FM 源定义及 README 表达缺口。按语言、规则、变化节奏和数据所有权设计边界，说明职责及明确不负责的范围；适用时使用 DDD 限界上下文。单一边界或不采用 DDD 时说明依据和替代设计，不按数量补造上下文。

## FM 映射

| FM Context/Entity/Rule ID | 目标边界 | 合并/拆分或不映射理由 | 规则及数据所有权 | 设计依据 |
| :------------------------ | :------- | :-------------------- | :--------------- | :------- |

模型事实引用稳定 ID，不重新维护规则。FM Context 不等于 DDD Bounded Context 或微服务，Fulfillment Context 也不自动成为软件模块。业务事实有冲突时反馈 Modeling 修订；简单胶水无模型时写明不适用依据，不补造合同。

## 集成关系

| 上游上下文 | 下游上下文 | 关系模式 | 通信方式 | 契约所有者 | 失败处理 |
| :--------- | :--------- | :------- | :------- | :--------- | :------- |

每条关系说明同步/异步选择、耦合风险和演进责任，按需使用 ACL、Published Language 等模式。没有跨边界协作时说明不适用，不为填表虚构关系。

## 上下文映射图

使用 Mermaid `flowchart` 展示方向、协议和 ACL 等边界组件。

## 集成约束

说明事务、一致性、幂等、超时、重试以及可观测性要求。引用统一 FM Context/Entity/Rule 与本工件的边界设计，说明领域、渠道及适用的 Contract/Fulfillment Context 如何映射到架构边界；不机械按每个 FM Context 拆服务。有跨上下文凭证时保留 Evidence Role 语义。API、数据库和消息机制不得写回 FM 核心模型。
