type: entity
id: contract.content-subscription
category: evidence
kind: contract
label: 内容产品订阅合同
contextRef: context.content-subscription
roleRefs:
  - role.subscriber
  - role.platform-subscription
attributes:
  - name: signed_at
    label: 签约时间
    valueType: timestamp
    required: true
    keyData: true
    meaning: 双方合同成立时间
